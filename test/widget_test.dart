// Smoke test placeholder. Real widget tests require Firebase to be
// initialized, which is out of scope for this template test.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    expect(1 + 1, 2);
  });
}
